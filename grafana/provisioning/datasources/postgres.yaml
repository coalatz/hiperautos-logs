apiVersion: 1

datasources:
  - name: PostgreSQL
    type: postgres
    uid: hiperautos_pg
    access: proxy
    url: postgres:5432
    database: logsdb
    user: admin
    secureJsonData:
      password: admin
    jsonData:
      sslmode: disable
      postgresVersion: 1500
    isDefault: true
